# Existing
streamlit
anthropic
sentence-transformers
python-dotenv
numpy

# Add these for new guards:
openai>=1.3.0
transformers>=4.35.0
torch>=2.1.0
accelerate>=0.24.0

# Optional (for NeMo)
nemoguardrails>=0.7.0

# Optional (for Llama Guard API)
replicate>=0.15.0